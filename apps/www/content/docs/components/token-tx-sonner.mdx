---
title: Token Tx Sonner
description: The Token Transaction Sonner is a component that displays real-time transaction status updates for users, including pending transactions, successful verifications, or errors encountered during processing.
component: true
---

<ComponentPreview
  name="token-tx-sonner-demo"
  className="[&_.preview>[data-orientation=vertical]]:sm:max-w-[70%]"
  description="A token transaction sonner"
/>

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx @token-kit/cli@latest add token-tx-sonner
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Copy and paste the ToastProvider into your app's root provider component to enable toast notifications globally.</Step>

```tsx
  import { ToastProvider } from "@/components/token-kit/token-tx-sonner";

  export function Providers({ children }: { children: React.ReactNode }) {
    return (
      <WagmiProvider config={config}>
        <ToastProvider />
      </WagmiProvider>
    );
  }
```

<Step>When a transaction is made in your application trigger the showTxSonner function to show the status.</Step>

<ComponentSource name="token-tx-sonner" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx
import { ToastProvider } from "@/components/token-kit/token-tx-sonner";
```

```tsx
export function Providers({ children }: { children: React.ReactNode }) {
  return (
    <WagmiProvider config={config}>
      <ToastProvider />
    </WagmiProvider>
  );
}
```

```tsx
import { showTxSonner } from "@/components/token-kit/token-tx-sonner";
```

```tsx
showTxSonner('0x6ab620baa39d4bf784a229c1aa4df0b25267863a4e623785d8937c48c6b4170d', 'https://etherscan.io/tx/', 5000);
```






